<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="description" content="{{post.description.full}}" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shahal</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        .btn {
            background-color: DodgerBlue;
            border: none;
            color: white;
            padding: 12px 16px;
            font-size: 16px;
            cursor: pointer;

        }

        .align-right {
            text-align: right;
            border: 0;
        }

        /* Darker background on mouse-over */
        .btn:hover {
            background-color: RoyalBlue;
        }

        .red {
            background-color: #c70000;
        }

        .red:hover {
            background-color: red;
        }
    </style>
</head>

<body>
    <div class="topnav">
        <a id="home" href="/admin">Home</a>
        <a id="new" href="/admin/compose">New</a>
        <a id="about" href="/edit-about">About</a>
        <a id="settings" href="/admin/settings">Settings</a>
        <div class="search-container">
            <form action="/search" method="GET">
                <input type="text" placeholder="Search.." name="q">
                <button type="submit"><i class="fa fa-search"></i></button>
            </form>
        </div>
    </div>

    <br>

    <div class="wrapper">
        <div class="news-single">
            <a href="/admin" class="anger-tag">&larr; Back</a>
            {{#if post}}
            <div class="align-right">
                <a class="btn" href="/admin/edit-post/{{post.id}}"><i class="fa fa-edit"></i></a>
                <a class="btn red" href="/admin/delete-post/{{post.id}}" style="margin-left: 6px;"><i
                        class="fa fa-trash"></i> </a>
            </div>
            <br>
            <h3>
                {{post.title.full}}
            </h3>
            <p>
                {{{post.description.full}}}
            </p>
            {{else}}
            <br>
            No posts found.
            {{/if}}

        </div>
    </div>


</body>

</html>